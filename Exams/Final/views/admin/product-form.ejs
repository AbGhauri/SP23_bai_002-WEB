<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Admin Panel - <%= action %> Product</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen">
  <nav class="bg-black text-white px-8 py-4 flex justify-between items-center">
    <div class="text-2xl font-bold">Admin Panel</div>
    <div>
      <a href="/admin/products" class="mr-6 hover:underline">Products</a>
      <a href="/admin/orders" class="hover:underline">Orders</a>
      <a href="/logout" class="ml-6 hover:underline text-red-400">Logout</a>
    </div>
  </nav>
  <main class="max-w-5xl mx-auto py-8">
    <h1 class="text-2xl font-bold mb-6"><%= action %> Product</h1>
    <form action="<%= action === 'Add' ? '/admin/products/new' : '/admin/products/' + product._id + '/edit' %>" method="POST" class="bg-white p-6 rounded shadow space-y-4">
      <div>
        <label class="block mb-1 font-semibold">Name</label>
        <input type="text" name="name" value="<%= product.name || '' %>" required class="border rounded px-3 py-2 w-full">
      </div>
      <div>
        <label class="block mb-1 font-semibold">Price</label>
        <input type="number" name="price" value="<%= product.price || '' %>" required class="border rounded px-3 py-2 w-full">
      </div>
      <div>
        <label class="block mb-1 font-semibold">Description</label>
        <textarea name="description" required class="border rounded px-3 py-2 w-full"><%= product.description || '' %></textarea>
      </div>
      <div>
        <label class="block mb-1 font-semibold">Main Image URL</label>
        <input type="text" name="mainImage" value="<%= product.mainImage || '' %>" required class="border rounded px-3 py-2 w-full">
      </div>
      <div>
        <label class="block mb-1 font-semibold">Hover Image URL</label>
        <input type="text" name="hoverImage" value="<%= product.hoverImage || '' %>" required class="border rounded px-3 py-2 w-full">
      </div>
      <div>
        <label class="block mb-1 font-semibold">Sizes (comma separated)</label>
        <input type="text" name="sizes" value="<%= product.sizes ? product.sizes.join(',') : '' %>" class="border rounded px-3 py-2 w-full">
      </div>
      <div>
        <label class="block mb-1 font-semibold">Colors (comma separated)</label>
        <input type="text" name="colors" value="<%= product.colors ? product.colors.join(',') : '' %>" class="border rounded px-3 py-2 w-full">
      </div>
      <button type="submit" class="bg-blue-600 text-white px-6 py-2 rounded"><%= action %> Product</button>
    </form>
  </main>
</body>
</html>